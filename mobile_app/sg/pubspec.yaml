name: sg
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0 # Updated to match your Dart version

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.0  # To save data locally
  intl: ^0.19.0               # To format Date & Time
  pdf: ^3.10.0
  path_provider: ^2.1.2
  share_plus: ^10.0.0



  # Camera & ML
  camera: ^0.11.0

  # ✅ USE THIS NEW VERSION (Fixes UnmodifiableUint8ListView error)
  tflite_flutter: ^0.12.1

  # ✅ UNCOMMENT THIS (We need it for the next step!)
  google_mlkit_pose_detection: ^0.12.0

  permission_handler: ^11.3.1
  fl_chart: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/seizure_lstm.tflite
    - assets/labels.txt
    - assets/pose_landmarker_heavy.task